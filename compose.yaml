services:
    proxy:
        build:
          context: .
          dockerfile: DockerFile
        ports:
            - "80:80"
        volumes:
            - type: bind
              source: ./proxy/nginx.conf
              target: /etc/nginx/conf.d/default.conf
              read_only: true
